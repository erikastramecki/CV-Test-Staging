# Backend
PORT=3001
COINVOYAGE_API_KEY=pk_live_lHCLPxP+kUVkOaUSN40gSzb5qBuYZPPw
COINVOYAGE_WEBHOOK_SECRET=whsec_replace_me
DATABASE_PATH=./server/database/transactions.db
FRONTEND_URL=http://localhost:5173

# Frontend (Vite)
VITE_COIN_VOYAGE_API_KEY=pk_live_lHCLPxP+kUVkOaUSN40gSzb5qBuYZPPw
VITE_API_URL=http://localhost:3001

# Casino admin token — required to access /casino/admin
# Without this set, the admin routes return 503.
CASINO_ADMIN_TOKEN=changeme

# Casino hot wallet private key — used to sign cross-chain payouts via CoinVoyage
# DANGER: Whoever has this key controls every dollar in the casino's wallet.
# - Use a fresh key for testing, fund it with a few dollars of USDC + tiny ETH for gas.
# - NEVER commit the real value, never share it, never put it in client code.
# - Without this set, the "Process Payout" admin button returns 503 and the
#   manual approve flow remains the only option.
# Format: 0x-prefixed 32-byte hex string (66 characters total).
CASINO_HOT_WALLET_PRIVATE_KEY=
